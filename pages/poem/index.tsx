import { NextPage } from "next";
import Link from "next/link";
import StaticCard from "../../components/StaticCard";

const StaticPoemPage = () => {
  return <StaticCard />;
};

export default StaticPoemPage;
